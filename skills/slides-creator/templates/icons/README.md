# SVG Icon Library

This directory provides **11,600+ high-quality SVG icons** across five libraries that can be directly embedded into SVG files generated by PPT Master. Four are stylistic libraries (pick **one** per deck); one is a brand-logo library (`simple-icons`) used as an inset alongside the chosen stylistic library.

## Libraries

| Library | Style | Count | viewBox | Prefix |
|---------|-------|-------|---------|--------|
| `chunk-filled` | fill 路 straight-line geometry (sharp corners, rectilinear) | 640 | `0 0 16 16` | `chunk-filled/` |
| `tabler-filled` | fill 路 bezier-curve forms (smooth, rounded contours) | 1000+ | `0 0 24 24` | `tabler-filled/` |
| `tabler-outline` | stroke / line | 5000+ | `0 0 24 24` | `tabler-outline/` |
| `phosphor-duotone` | duotone 路 single color + 0.2 opacity backplate (soft depth) | 1200+ | `0 0 256 256` | `phosphor-duotone/` |
| `simple-icons` | **brand logos** (real company / product marks) 鈥?single-color silhouettes, color in via `fill` | 3400+ | `0 0 24 24` | `simple-icons/` |

---

## Usage

Use placeholder syntax **during SVG generation**:

```xml
<!-- chunk-filled (sharp, geometric 鈥?tech/engineering/enterprise tone) -->
<use data-icon="chunk-filled/home" x="100" y="200" width="48" height="48" fill="#0076A8"/>

<!-- tabler-filled (rounded, organic 鈥?lifestyle/health/home tone) -->
<use data-icon="tabler-filled/home" x="100" y="200" width="48" height="48" fill="#0076A8"/>

<!-- tabler-outline (light, line-art 鈥?refined screen-only showcases) -->
<use data-icon="tabler-outline/home" x="100" y="200" width="48" height="48" fill="#0076A8"/>

<!-- phosphor-duotone (soft depth 鈥?single color renders the backplate at 20% opacity) -->
<use data-icon="phosphor-duotone/house" x="100" y="200" width="48" height="48" fill="#0076A8"/>

<!-- simple-icons (brand logo 鈥?used alongside the deck's primary library, not as a substitute) -->
<use data-icon="simple-icons/github" x="100" y="200" width="48" height="48" fill="#181717"/>
```

**Attributes**:
- `data-icon` 鈥?`<library>/<icon-name>` (filename without `.svg`)
- `x`, `y` 鈥?Position
- `width`, `height` 鈥?Size (recommend 32鈥?8px for legibility)
- `fill` 鈥?Color

`finalize_svg.py` auto-embeds all placeholders during post-processing. To run manually:

```bash
python3 scripts/svg_finalize/embed_icons.py svg_output/*.svg
```

---

## Searching for Icons

Use `ls | grep` 鈥?zero token cost:

```bash
ls skills/slides-creator/templates/icons/chunk-filled/ | grep home
ls skills/slides-creator/templates/icons/tabler-filled/ | grep home
ls skills/slides-creator/templates/icons/tabler-outline/ | grep chart
ls skills/slides-creator/templates/icons/phosphor-duotone/ | grep house
ls skills/slides-creator/templates/icons/simple-icons/ | grep github
```

---

## Style Rules

**No default library 鈥?actively choose based on the deck's visual needs.** Read the source material first, then pick the library whose visual character best serves the presentation. Each library has a distinct visual personality:

- **`chunk-filled`** 鈥?**fill** style, built from straight-line commands only (M/L/H/V/Z). Sharp, precise right angles; rectilinear geometry; structured and highly legible at small sizes. Visual weight: heavy, solid, architectural.
- **`tabler-filled`** 鈥?**fill** style, built from bezier curves and arcs (C/A). Smooth, rounded, organic contours; warmer and softer than `chunk-filled`. Visual weight: medium, approachable.
- **`tabler-outline`** 鈥?**stroke** style (line art, default stroke-width 2). Airy, refined, lightweight; uses negative space. Visual weight: light, elegant. Best for screen-only viewing since thin strokes may become hard to read when printed or projected.
- **`phosphor-duotone`** 鈥?**duotone** style; main shape at full opacity plus a backplate of the same color at 20% opacity, producing a soft sense of depth. Visual weight: medium, layered, contemporary.

> **Two axes to consider when choosing**:
> 1. **Geometry**: straight lines (`chunk-filled`) vs. curves (`tabler-filled` / `phosphor-duotone`) vs. open strokes (`tabler-outline`)
> 2. **Visual weight**: heavy solid (`chunk-filled`) 鈫?medium solid (`tabler-filled`) 鈫?medium layered (`phosphor-duotone`) 鈫?light stroke (`tabler-outline`)

**One presentation = one stylistic library.** Pick `chunk-filled` / `tabler-filled` / `tabler-outline` / `phosphor-duotone` at the start and use it exclusively throughout for generic icons (home, chart, users, etc.). If the chosen library doesn't have an exact icon, find the closest available alternative within that same library 鈥?never cross stylistic libraries to fill a gap.

**Brand-logo exception (`simple-icons`).** `simple-icons` is **not a stylistic library** and does not participate in the "one library" rule. Its job is brand recognition 鈥?Slack's purple, GitHub's cat, AWS's color 鈥?which is intentionally heterogeneous. Use it **alongside** the chosen stylistic library, but **only** for actual company / product / service brand marks. Do **not** reach for it as a substitute when the chosen stylistic library lacks a generic icon.

| Use `simple-icons` for | Do NOT use `simple-icons` for |
|------------------------|-------------------------------|
| Customer / partner / ecosystem logos on a "trusted by" page | Generic concepts (home, chart, settings, etc.) |
| Tech stack icons on architecture / integration diagrams | Replacing a missing icon in `chunk-filled` / `tabler-*` / `phosphor-duotone` |
| Social media handles in a footer | Decorative / illustrative purposes |

鈿狅笍 Do **not** mix icons from different **stylistic** libraries (`chunk-filled` / `tabler-filled` / `tabler-outline` / `phosphor-duotone`). `simple-icons` is the sole exception and may co-exist as a brand-logo inset 鈥?see the brand-logo exception above.
